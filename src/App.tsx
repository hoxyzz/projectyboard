// This component is no longer used - routing is now handled by Next.js App Router
// Page components are located in src/app/ and views are in src/views/
// The providers and layout wrapper are handled in src/app/layout.tsx

export default function App() {
  return null;
}
